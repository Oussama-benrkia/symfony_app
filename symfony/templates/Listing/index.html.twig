{% extends 'layout.html.twig' %}
{% block title %}   
    Post
{% endblock %}

{% block content %}
        {% include "./Components/section.html.twig" %}

<main>
                {% include "./Components/search.html.twig" with {"search":search} %}

         {% if data.items %}
          <div class="lg:grid lg:grid-cols-2 gap-4 space-y-4 md:space-y-0 mx-4">
            {% for item in data.items %}
                {% include "./Components/card.html.twig"  with  {item} %}
            {% endfor %}
            </div> 
            <nav class="flex justify-center">
    <ul class="inline-flex -space-x-px text-base h-10">
        <li>
            <a href="/?page=1" class="flex items-center justify-center px-4 h-10 ms-0 leading-tight text-gray-500 bg-white border border-e-0 border-gray-300 rounded-s-lg hover:bg-red-100 hover:text-gray-700">
                First
            </a>
        </li>
        <li>
            <a href="?page={{ data.currentPageNumber - 1 }}" class="flex items-center justify-center px-4 h-10 leading-tight text-gray-500 bg-white border border-gray-300 hover:bg-red-100 hover:text-gray-700">
                <
            </a>
        </li>
        <li>
            <a href="?page={{ data.currentPageNumber + 1 }}" class="flex items-center justify-center px-4 h-10 leading-tight text-gray-500 bg-white border border-gray-300 hover:bg-red-100 hover:text-gray-700">
                >
            </a>
        </li>
        <li>
            <a href="/?page={{ data.pageCount }}" class="flex items-center justify-center px-4 h-10 leading-tight text-gray-500 bg-white border border-gray-300 rounded-e-lg hover:bg-red-100 hover:text-gray-700">
                Last
            </a>
        </li>
    </ul>
</nav>
         {% endif %}

</div>


        </main>
{% endblock %}